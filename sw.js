// This is a minimal service worker. 
// It simply listens for network requests so Chrome registers the app as a PWA.
self.addEventListener('fetch', function(event) {
    // Leave empty for basic installation requirements
});